/* ===================================================================
   THE MAKHANA - Supabase config
   Paste your project's URL and ANON (public) key below.
   Find them in Supabase > Project Settings > API.
   The anon key is safe to expose in the browser - Row Level Security
   (see supabase-schema.sql) is what keeps customer data private.
   =================================================================== */
window.SUPABASE_URL = "YOUR_SUPABASE_URL";        // e.g. https://abcdxyz.supabase.co
window.SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
